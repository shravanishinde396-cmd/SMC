<!DOCTYPE html>
<html lang="en" class="h-full bg-slate-50 antialiased">

<head>
    <meta charset="UTF-8">
    <title>Patient History | <%= profile.name %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Inter', 'sans-serif'] },
                    colors: {
                        brand: { 50: '#ecfdf5', 100: '#d1fae5', 500: '#10b981', 600: '#059669', 700: '#047857', 900: '#064e3b' },
                        slate: { 850: '#1e293b' }
                    }
                }
            }
        }
    </script>
</head>

<body class="flex flex-col min-h-full text-slate-600">

    <%- include("../partials/navbar", { hospital: hospital || null }) %>

    <main class="flex-grow py-8 px-4 sm:px-6 lg:px-8">
        <div class="max-w-5xl mx-auto">

            <div class="mb-6 flex items-center justify-between">
                <nav class="flex items-center space-x-2 text-sm text-slate-500">
                    <a href="/hospital/dashboard" class="hover:text-brand-600 transition-colors">Dashboard</a>
                    <svg class="h-4 w-4 text-slate-300" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/></svg>
                    <a href="/hospital/patients" class="hover:text-brand-600 transition-colors">Patients</a>
                    <svg class="h-4 w-4 text-slate-300" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd"/></svg>
                    <span class="text-slate-900 font-semibold bg-slate-100 px-2 py-0.5 rounded text-xs">History</span>
                </nav>
                <a href="/hospital/patients" class="inline-flex items-center px-3 py-1.5 border border-slate-300 text-xs font-medium rounded-lg text-slate-700 bg-white hover:bg-slate-50 transition-all">
                    Back to List
                </a>
            </div>

            <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden mb-8">
                <div class="p-6 sm:p-8 flex flex-col sm:flex-row gap-6 items-start sm:items-center justify-between">
                    
                    <div class="flex items-center gap-5">
                        <div class="h-16 w-16 rounded-full bg-slate-100 flex items-center justify-center border-2 border-white shadow-sm ring-1 ring-slate-200">
                            <span class="text-2xl font-bold text-slate-400">
                                <%= profile.name.charAt(0).toUpperCase() %>
                            </span>
                        </div>
                        <div>
                            <h1 class="text-2xl font-bold text-slate-900 tracking-tight"><%= profile.name %></h1>
                            <div class="flex flex-wrap gap-x-4 gap-y-1 mt-1 text-sm text-slate-500">
                                <span class="flex items-center gap-1.5">
                                    <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                                    <%= profile.age || "—" %> yrs / <%= profile.gender || "—" %>
                                </span>
                                <span class="flex items-center gap-1.5">
                                    <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path></svg>
                                    <%= profile.phone || "—" %>
                                </span>
                            </div>
                        </div>
                    </div>

                    <div>
                        <a href="/hospital/patients/profile/<%= profile._id %>/pdf" class="inline-flex items-center px-4 py-2 border border-brand-200 shadow-sm text-sm font-medium rounded-lg text-brand-700 bg-brand-50 hover:bg-brand-100 focus:outline-none transition-all">
                            <svg class="mr-2 -ml-1 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                            Download History PDF
                        </a>
                    </div>
                </div>
            </div>

            <div class="relative py-4">
                <div class="absolute inset-0 flex items-center" aria-hidden="true">
                    <div class="w-full border-t border-slate-200"></div>
                </div>
                <div class="relative flex justify-center">
                    <span class="px-3 bg-slate-50 text-sm font-medium text-slate-500 uppercase tracking-wider">Clinical Timeline</span>
                </div>
            </div>

            <div class="space-y-6">

                <% if (visits.length === 0) { %>
                    <div class="text-center py-12 bg-white rounded-xl border border-slate-200 border-dashed">
                        <svg class="mx-auto h-12 w-12 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        <h3 class="mt-2 text-sm font-medium text-slate-900">No medical history</h3>
                        <p class="mt-1 text-sm text-slate-500">There are no recorded visits for this patient profile yet.</p>
                    </div>
                <% } %>

                <% visits.forEach((v, i) => { %>
                    <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden hover:shadow-md transition-shadow">
                        
                        <div class="px-6 py-4 bg-slate-50/50 border-b border-slate-100 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                            <div class="flex items-center gap-3">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-bold border 
                                    <%= v.patientType === 'IPD' ? 'bg-rose-100 text-rose-800 border-rose-200' : 'bg-blue-100 text-blue-800 border-blue-200' %>">
                                    <%= v.patientType %>
                                </span>
                                <span class="text-sm font-medium text-slate-900">
                                    <%= v.admissionDate ? v.admissionDate.toDateString() : "Date Not Recorded" %>
                                </span>
                            </div>
                            
                            <% if (v.patientType === "IPD") { %>
                                <div class="flex items-center text-xs">
                                    <% if (v.dischargeDate) { %>
                                        <span class="text-emerald-600 font-medium flex items-center">
                                            <svg class="w-3.5 h-3.5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                            Discharged
                                        </span>
                                    <% } else { %>
                                        <span class="text-amber-600 font-medium flex items-center">
                                            <span class="relative flex h-2 w-2 mr-1.5">
                                              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-amber-400 opacity-75"></span>
                                              <span class="relative inline-flex rounded-full h-2 w-2 bg-amber-500"></span>
                                            </span>
                                            Currently Admitted
                                        </span>
                                    <% } %>
                                </div>
                            <% } %>
                        </div>

                        <div class="p-6 grid grid-cols-1 md:grid-cols-3 gap-6">
                            
                            <div class="md:col-span-1 space-y-4">
                                <div>
                                    <label class="text-xs font-semibold text-slate-400 uppercase tracking-wide">Diagnosis</label>
                                    <p class="text-sm font-medium text-slate-900 mt-1"><%= v.disease || "—" %></p>
                                </div>
                                <div>
                                    <label class="text-xs font-semibold text-slate-400 uppercase tracking-wide">Attending Doctor</label>
                                    <div class="flex items-center gap-2 mt-1">
                                        <div class="h-6 w-6 rounded-full bg-slate-100 flex items-center justify-center text-xs font-bold text-slate-500">
                                            Dr
                                        </div>
                                        <p class="text-sm text-slate-700"><%= v.doctor ? v.doctor.name : "Unassigned" %></p>
                                    </div>
                                </div>
                                <div class="pt-4">
                                    <a href="/hospital/patients/<%= v._id %>/prescription" class="text-sm font-medium text-brand-600 hover:text-brand-800 hover:underline inline-flex items-center">
                                        Manage Prescription &rarr;
                                    </a>
                                </div>
                            </div>

                            <div class="md:col-span-2 bg-slate-50 rounded-lg p-4 border border-slate-100">
                                <h6 class="text-xs font-bold text-slate-500 uppercase tracking-wide mb-3 flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.384-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path></svg>
                                    Prescribed Medication
                                </h6>
                                
                                <% if (v.prescription && v.prescription.length > 0) { %>
                                    <ul class="space-y-3">
                                        <% v.prescription.forEach(p => { %>
                                            <li class="flex items-start justify-between text-sm group">
                                                <div class="flex items-center gap-2">
                                                    <span class="w-1.5 h-1.5 rounded-full bg-slate-300 group-hover:bg-brand-500 transition-colors mt-1.5"></span>
                                                    <span class="font-medium text-slate-700">
                                                        <%= p.medicine ? p.medicine.name : "Unknown Medicine" %>
                                                    </span>
                                                </div>
                                                <div class="text-slate-500 text-xs font-mono bg-white px-2 py-0.5 rounded border border-slate-200">
                                                    Qty: <%= p.quantity %> • <%= p.dosage || "As advised" %>
                                                </div>
                                            </li>
                                        <% }) %>
                                    </ul>
                                <% } else { %>
                                    <div class="text-center py-4">
                                        <p class="text-xs text-slate-400 italic">No medicines added to this record yet.</p>
                                    </div>
                                <% } %>
                            </div>

                        </div>
                    </div>
                <% }) %>

            </div>

        </div>
    </main>

    <%- include("../partials/footer") %>

</body>
</html>